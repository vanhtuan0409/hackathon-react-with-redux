.bottom-button-group {
  position: absolute;
  bottom: 0;
  left: 0;
  width 100%;
  height 40px;
  line-height 40px;
  display: flex;
  flex-direction: row;
}
.bottom-button-group button {
  height: 100%!important;
}
